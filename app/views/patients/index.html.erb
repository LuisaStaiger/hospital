<h1>Index of all the sicko patients</h1>

<ul>
  <% @uncured_patients.each do |patient| %>
    <li><%= link_to "#{patient.first_name} #{patient.last_name}", patient_path(patient) %></li>
  <% end %>
</ul>

<%= link_to "add a new patient", new_patient_path %>
<%= link_to "homepage", root_path %>
<%= link_to "overview patient appointments", appointments_path %>
